# ── Ядро ──────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0

# ── Облачные хранилища ─────────────────────────────────────────────────
webdavclient3>=3.14      # Nextcloud / WebDAV
boto3>=1.28              # Amazon S3, Yandex Object Storage, MinIO

# ── Аудио / видео ─────────────────────────────────────────────────────
ffmpeg-python>=0.2.0     # Python-обёртка для ffmpeg
soundfile>=0.12          # чтение/запись WAV, FLAC
librosa>=0.10            # аудиоанализ (опционально)

# ── Транскрибация ─────────────────────────────────────────────────────
openai-whisper>=20231117 # оригинальный Whisper (устанавливает torch автоматически)
faster-whisper>=1.0      # CTranslate2-версия, в 2–4× быстрее
transformers>=4.40       # wav2vec2, SeamlessM4T и другие HF-модели
torchaudio>=2.1          # загрузка аудио, ресемплинг

# ── Диаризация ────────────────────────────────────────────────────────
pyannote.audio>=3.1      # SOTA диаризация (требует HF-токен)
simple_diarizer>=0.0.8   # без токена

# ── VAD ───────────────────────────────────────────────────────────────
# silero-vad загружается через torch.hub, pip-пакет не нужен
webrtcvad>=2.0.10        # алгоритмический VAD от Google

# ── Метрики качества речи ─────────────────────────────────────────────
# torchaudio уже выше — содержит PESQ и STOI
jiwer>=3.0               # WER, CER

# ── Шумоподавление ────────────────────────────────────────────────────
noisereduce>=3.0         # спектральное вычитание
speechbrain>=1.0         # DNN-шумоподавление (SepFormer и др.)
# demucs — устанавливайте отдельно: pip install demucs

# ── Постобработка ─────────────────────────────────────────────────────
spacy>=3.7               # NLP: токенизация, NER
clean-text>=0.6          # нормализация текста

# ── Отчёты ────────────────────────────────────────────────────────────
jinja2>=3.1
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
weasyprint>=60           # рендеринг PDF (опционально)

# ── Jupyter ───────────────────────────────────────────────────────────
jupyterlab>=4.0
ipywidgets>=8.0
tqdm>=4.66
